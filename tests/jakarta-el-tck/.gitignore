# Reports of a native test binary run by hand from this directory. The build writes its own under build/.
# Kept here rather than in the root .gitignore, which the project template sync overwrites.
/test-results-native/
