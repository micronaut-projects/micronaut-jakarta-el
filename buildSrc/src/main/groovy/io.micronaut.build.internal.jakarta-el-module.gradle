plugins {
    id 'io.micronaut.build.internal.jakarta-el-base'
    id 'io.micronaut.build.internal.module'
    id 'io.micronaut.build.internal.convention-nullaway'
    id 'io.micronaut.build.internal.convention-no-reflection'
}

micronautBuild {
    // there is no released version to compare against yet
    binaryCompatibility {
        enabled = false
    }
}

dependencies {
    testRuntimeOnly(libs.junit.platform.launcher)
}
